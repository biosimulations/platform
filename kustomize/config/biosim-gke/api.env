# Deployment-specific CORS origins. Hardcoded list in api/main.py covers only
# local-dev loopbacks and cross-org trusted services; the deployed frontend
# host(s) must be listed here per overlay.
CORS_EXTRA_ORIGINS=https://biosim.biosimulations.org
