# Dev-frontend on the RKE cluster, pointed at the prod backend.
BASE_URL=https://biosim-dev.biosimulations.org
API_URL=https://api.biosim.biosimulations.org
# No in-namespace api Service, so SSR fetches also go through the public URL.
API_URL_INTERNAL=https://api.biosim.biosimulations.org
BIOSIMULATIONS_API_URL=https://api.biosimulations.org
